/**
 * 应用元信息（SSOT）
 * 注意：projectRootDir 已迁移至用户设置（useSettings → general.projectRootDir）
 */
export const APP_NAME = 'PG素材管理系统'
export const APP_VERSION = 'V2.6.1'
export const APP_DEVELOPER = 'Fuchikami'

/** @deprecated 已无内容，仅保留以防旧代码引用 */
export const APP_CONFIG = {} as const
